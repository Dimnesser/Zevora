# ZEVORA

Игровая платформа в стиле кейс-сайтов CS2: открытие кейсов, апгрейд
предметов, инвентарь, вывод скинов и закрытая партнёрская программа.

Розыгрыш, баланс и инвентарь живут **на сервере**. Клиент показывает
анимацию уже известного результата и не может на него повлиять.

---

## Запуск

```bash
npm install
npm run dev        # http://localhost:3000
```

База создаётся и заполняется автоматически при первом запросе — никакого
отдельного шага миграции нет.

```bash
npm run build && npm start   # продакшен
npm run typecheck            # tsc --noEmit
npm run db:reset             # удалить базу и пересоздать с нуля
npm run build:cases          # отрисовать 25 кейсов в public/cases
npm run build:cases:data     # пересобрать дроп-таблицы
npm run build:showcase       # статическая витрина в docs/
npm run import:skins         # импорт реальных рендеров CS2
npm run verify:images        # проверка происхождения, файлов, уникальности
npm run verify:browser       # проверка тех же 20 скинов в браузере
```

**Демо-аккаунты** (создаются сидом):

| Ник | Пароль | Роль |
|---|---|---|
| `dimnesser` | `zevora123` | владелец — доступ в `/admin` |
| `player` | `player123` | обычный игрок |

Кнопки быстрого входа есть на `/login`.

---

## Стек

| Слой | Выбор |
|---|---|
| Фреймворк | Next.js 15 (App Router, route handlers) |
| Язык | TypeScript (strict) |
| База | SQLite через `better-sqlite3` |
| Авторизация | scrypt + httpOnly cookie-сессии |
| Стили | Tailwind CSS 3 |
| Анимации | Framer Motion |
| Звук | Web Audio API (синтез, без файлов) |

SQLite выбран как встроенный движок: настоящие транзакции, внешние ключи
и индексы при нулевой настройке. Слой доступа к данным изолирован в
`lib/server/`, схема написана переносимо — переезд на PostgreSQL сводится
к замене типов и драйвера.

---

## Структура

```
app/api/              route handlers (20 эндпоинтов)
lib/server/           schema.sql, db, auth, cases, inventory, ledger,
                      openings, rng, wear, stats, seed, presenters, http
lib/client/           типизированный API-клиент, сессия, звук
components/           UI: cases, items, upgrade, wallet, admin, partners…
scripts/              тесты, симуляции, обслуживание БД
```

---

## База данных

| Таблица | Назначение |
|---|---|
| `users` | аккаунт, баланс (integer), роль, партнёрский статус |
| `sessions` | SHA-256 токена сессии, срок действия |
| `rarities` | редкости: цвет, эффект, вес по умолчанию, порядок |
| `skins` | каталог: `market_hash_name`, `image_url`, `image_source`, `image_status`, редкость, базовая цена |
| `skin_images` | журнал изображений: URL, источник, HTTP-статус, content-type, размер, ширина/высота |
| `cases` | кейс: slug, цена, активность, партнёрский доступ |
| `case_items` | связка кейс↔скин: **вес**, диапазон float, StatTrak |
| `inventory_items` | экземпляры предметов игрока: износ, float, статус |
| `case_openings` | каждое открытие + **выигрышный билет и общий вес** |
| `transactions` | двойная запись: сумма и баланс после операции |
| `idempotency_keys` | защита от повторного выполнения, PK `(user_id, key)` |
| `promo_codes` / `promo_redemptions` | промокоды и однократность применения |

Внешние ключи включены на каждом соединении, индексы стоят на всех FK и
на `created_at DESC` для лент.

Скин не дублируется: один `skins.id` может входить в любое число кейсов
через `case_items`, а `UNIQUE (case_id, skin_id)` запрещает дубль внутри
одного кейса.

---

## Деньги

Все суммы — **целые числа в копейках** (`*_minor`). Ни одна денежная
величина не хранится и не считается во float. На баланс стоит
`CHECK (balance_minor >= 0)`, а списание выполняется условным `UPDATE`:

```sql
UPDATE users SET balance_minor = balance_minor - ?
 WHERE id = ? AND balance_minor >= ?
```

Если `changes = 0`, средств не хватило — проверка и запись это одна
операция, поэтому две параллельные попытки не могут обе пройти.

---

## Как работает открытие кейса

```
POST /api/cases/:slug/open        + заголовок Idempotency-Key
        │
        ├─ requireUser()                      сессия из httpOnly cookie
        ├─ rateLimit(user)                    60 открытий / 10 с
        │
        └─ transact(() => {                   BEGIN IMMEDIATE
             1. ключ идемпотентности уже есть → вернуть прошлый ответ
             2. прочитать кейс из БД          ← цена берётся здесь
             3. условное списание баланса     ← не хватило → 402
             4. загрузить дроп-таблицу, посчитать сумму весов
             5. crypto.randomInt(0, totalWeight) → предмет
             6. разыграть float, износ, StatTrak, цену экземпляра
             7. INSERT inventory_items
             8. INSERT case_openings (+ roll, total_weight)
             9. INSERT transactions
            10. сохранить ответ под ключом идемпотентности
           })                                 COMMIT / полный откат
        │
        └─ ответ клиенту → клиент запускает ленту к уже известному предмету
```

Тело запроса не содержит ничего, что влияет на исход: ни цены, ни
`skin_id`, ни шансов. Единственные входные данные — slug в пути и ключ
идемпотентности в заголовке.

---

## Система вероятностей

Вес — целое число «билетов». Вероятность предмета равна `вес / Σ весов`
по кейсу. Те же самые числа показываются на странице кейса до открытия.

Розыгрыш использует `crypto.randomInt(0, totalWeight)` — CSPRNG с
отбраковкой, без модульного смещения, которое даёт наивный
`Math.floor(Math.random() * n)`.

Каждое открытие сохраняет `roll` и `total_weight`, поэтому результат
можно перепроверить постфактум. Билет виден на экране выигрыша и в
истории открытий.

Экономика кейсов (`npm run test:probability`). Дроп-таблицы не написаны
руками: `npm run build:cases:data` подбирает предметы по ценовому коридору
кейса и решает веса под целевую маржу, а кейс, чей пул не тянет свою цену,
сборка отклоняет.

| Кейс | Цена | Предметов | Ср. ценность | Маржа | Джекпот |
|---|---|---|---|---|---|
|  Первый заход | 19 ₽ | 12 | 12 ₽ | 36% | AK-47 | Redline · 0.001% |
|  Нулевой отсек | 39 ₽ | 13 | 24 ₽ | 38% | AWP | Hyper Beast · 0.007% |
|  Жестянка | 69 ₽ | 13 | 43 ₽ | 37% | AK-47 | Vulcan · 0.018% |
|  Серый сектор | 99 ₽ | 14 | 61 ₽ | 38% | AWP | Lightning Strike · 0.002% |
|  Пыльная смена | 149 ₽ | 14 | 91 ₽ | 39% | AWP | Asiimov · 0.018% |
|  Час волка | 249 ₽ | 15 | 149 ₽ | 40% | M4A1-S | Printstream · 0.038% |
|  Мокрый асфальт | 399 ₽ | 15 | 239 ₽ | 40% | M4A1-S | Printstream · 0.064% |
|  Кислотный дождь | 599 ₽ | 15 | 365 ₽ | 39% | ★ Bayonet | Tiger Tooth · 0.028% |
|  Неоновый квартал | 899 ₽ | 16 | 557 ₽ | 38% | ★ M9 Bayonet | Marble Fade · 0.020% |
|  Холодная сварка | 1 290 ₽ | 16 | 775 ₽ | 40% | ★ Karambit | Doppler · 0.035% |
|  Красная смена | 1 790 ₽ | 16 | 1 074 ₽ | 40% | ★ Karambit | Doppler · 0.054% |
|  Белый шум | 2 490 ₽ | 17 | 1 518 ₽ | 39% | ★ Sport Gloves | Pandora's Box · 0.028% |
|  Чёрный январь | 3 490 ₽ | 17 | 2 095 ₽ | 40% | M4A4 | Howl · 0.019% |
|  Дальний выстрел | 4 490 ₽ | 10 | 2 603 ₽ | 42% | AWP | Dragon Lore · 0.138% |
|  Пурпурный разлом | 4 990 ₽ | 17 | 2 943 ₽ | 41% | M4A4 | Howl · 0.022% |
|  Золотая жила | 6 990 ₽ | 18 | 4 126 ₽ | 41% | M4A4 | Howl · 0.020% |
|  Глубокий эшелон | 9 900 ₽ | 18 | 5 741 ₽ | 42% | M4A4 | Howl · 0.030% |
|  Ядро | 13 900 ₽ | 18 | 8 065 ₽ | 42% | AWP | Dragon Lore · 0.050% |
|  Последний свет | 19 900 ₽ | 18 | 11 541 ₽ | 42% | AWP | Dragon Lore · 0.125% |
|  Теневой фонд | 27 900 ₽ | 17 | 15 906 ₽ | 43% | AWP | Dragon Lore · 0.145% |
|  Корона севера | 39 900 ₽ | 16 | 22 736 ₽ | 43% | AWP | Dragon Lore · 0.154% |
|  Алмазный фонд | 54 900 ₽ | 15 | 31 291 ₽ | 43% | AWP | Dragon Lore · 0.294% |
|  Легенда 2013 | 79 900 ₽ | 14 | 44 745 ₽ | 44% | AWP | Dragon Lore · 0.563% |
|  Только сталь | 94 900 ₽ | 7 | 59 359 ₽ | 37% | ★ Sport Gloves | Pandora's Box · 0.002% |
|  Зал основателей | — | 15 | 115 ₽ | — | AWP | Dragon Lore · 0.001% |

χ² тест согласия при 1 000 000 розыгрышей на каждый из 25 кейсов
(25 млн розыгрышей): все проходят при p = 0.001.

---|---|---|---|
| Стартовый набор | 49 ₽ | 30 ₽ | 39% |
| Ночной рейд | 299 ₽ | 171 ₽ | 43% |
| Neon Division | 899 ₽ | 616 ₽ | 31% |
| Asiimov Protocol | 2 490 ₽ | 1 543 ₽ | 38% |
| Кузня клинков | 7 990 ₽ | 4 475 ₽ | 44% |
| Legacy Vault | 19 900 ₽ | 11 939 ₽ | 40% |

Закрытый партнёрский `Zevora Vault` бесплатный, поэтому маржи у него нет.

---

## Инвентарь

Предмет — это **экземпляр** скина, а не ссылка на каталог: при выпадении
разыгрываются float и износ, от них считается цена, StatTrak добавляет
25%. Поэтому два одинаковых скина в инвентаре стоят по-разному.

Множители износа: Factory New ×1.18, Minimal Wear ×1.06, Field-Tested
×0.92, Well-Worn ×0.81, Battle-Scarred ×0.72.

Список читается по `user_id` **из сессии** — параметра «чей инвентарь» в
API нет. Продажа и вывод сужены до `user_id = ? AND status = 'owned'`,
поэтому чужой предмет не найдётся, а повторная продажа своего вернёт 409.

---

## API

| Метод | Путь | Доступ |
|---|---|---|
| POST | `/api/auth/register` · `/login` · `/logout` | публичный |
| GET | `/api/auth/me` | публичный |
| GET | `/api/cases` · `/api/cases/:slug` | публичный |
| POST | `/api/cases/:slug/open` | игрок |
| GET | `/api/inventory` | игрок |
| POST | `/api/inventory/sell` · `/withdraw` | игрок |
| GET | `/api/openings` (`?scope=all` — владелец) | игрок |
| GET | `/api/live` · `/api/leaderboard` | публичный |
| GET | `/api/me/stats` · `/api/transactions` | игрок |
| POST | `/api/wallet/deposit` | игрок |
| POST | `/api/bonuses/daily` · `/promo` · `/registration` | игрок |
| POST | `/api/shop/buy` · `/api/upgrade` | игрок |
| GET POST | `/api/admin/cases` | владелец |
| GET PATCH DELETE | `/api/admin/cases/:id` | владелец |
| GET POST | `/api/admin/cases/:id/items` | владелец |
| PATCH DELETE | `/api/admin/cases/:id/items/:itemId` | владелец |
| GET | `/api/admin/cases/:id/stats` | владелец |
| GET POST | `/api/admin/rarities` | владелец |
| GET | `/api/admin/skins` · `/api/admin/stats` · `/api/admin/users` | владелец |
| PUT PATCH DELETE | `/api/admin/users/:id/partner` | владелец |

---

## Безопасность

| Угроза | Защита |
|---|---|
| Подмена цены через DevTools | цена читается из БД внутри транзакции; в теле запроса поля цены нет |
| Подмена `skin_id` / результата | предмет выбирает сервер; клиент получает готовый ответ |
| Повторное открытие, двойной клик | `Idempotency-Key`, PK `(user_id, key)` — повтор возвращает прежний ответ |
| Двойное списание, гонки | `BEGIN IMMEDIATE` + условный `UPDATE` баланса |
| Отрицательный баланс | `CHECK (balance_minor >= 0)` |
| Чужой инвентарь | запросы сужены по `user_id` из сессии |
| Выдача без оплаты | предмет пишется только внутри той же транзакции, что и списание |
| Прямые запросы к API | `requireUser` / `requireOwner` на каждом мутирующем эндпоинте |
| SQL-инъекции | подготовленные выражения; в SQL подставляются только имена колонок из кода |
| Перебор пароля / флуд | лимиты частоты; одинаковый ответ на неверный ник и пароль |
| Утечка хеша пароля | ответы собираются в `presenters.ts` |
| CSRF | сессионная cookie `SameSite=Lax`, httpOnly |

---

## Кейсы и их оформление

25 кейсов, от 19 ₽ до 94 900 ₽, плюс закрытый партнёрский. Каждому —
свой корпус, декаль и потолок дроп-таблицы, чтобы уровни отличались не
только ценой.

### Дизайн

Модель собрана в Figma — файл «Zevora — Cases»: эталонный фрейм
`master/case` и 25 перекрашенных клонов. Это ударопрочный кейс в
3/4 проекции: отдельные крышка и корпус, литые угловые бамперы,
металлические защёлки, утопленная ручка, рёбра, вентиляция в крышке и
утопленная площадка под декаль.

`scripts/build-case-art.mjs` —产 порт этого файла: та же геометрия, та же
схема затенения (фаски, внутренние тени, блик на крышке, двойная
контактная тень) в SVG, растеризация в WebP через `sharp`. Ассеты
собираются из репозитория и не зависят от экспорта из Figma.

```bash
npm run build:cases              # 25 кейсов × 3 слоя → public/cases
npm run build:cases -- --scale 2 # в 2× разрешении
```

Три слоя на кейс: `<slug>.webp` для карточек, `<slug>-body.webp` и
`<slug>-lid.webp` — те же 640 × 480, чтобы крышку можно было отделить.

### Анимация открывания

`components/cases/CaseUnlock.tsx` играет между ответом сервера и лентой:
кейс дёргается, защёлки отпускают, крышка уходит вверх с наклоном, из
шва бьёт свет цвета корпуса и летят искры. Дальше запускается рулетка.

Анимация ничего не решает — предмет уже определён сервером, `onDone`
висит на таймере, а страховочный таймаут доводит до ленты, даже если
анимация не доиграла. Режим «Быстрое открытие» пропускает её целиком.

### Каталог

Карточка кейса показывает сам кейс и три самых дорогих предмета из его
таблицы — настоящими рендерами, а не подписями.

---

## Изображения скинов

### SOURCE

Проверены и отброшены как недоступные из этой среды (egress-политика,
HTTP 000/403): все хосты Steam — `community.cloudflare.steamstatic.com`,
`community.akamai.steamstatic.com`, `community.fastly.steamstatic.com`,
`steamcdn-a.akamaihd.net`, `cdn.steamstatic.com`, `media.steampowered.com`,
`steamcommunity.com`; а также `cdn.csgoskins.gg`, `csgostash.com`,
`steamwebapi.com`, `csmarketcap.com`, `bymykel.github.io`,
`cdn.jsdelivr.net`, `unpkg.com`, `cdn.skinhub.gg`, `cdn.cstrike.app`.

Проверены и отброшены по содержимому:
`ByMykel/counter-strike-image-tracker` (2 466 файлов — стикеры, кейсы,
иконки коллекций, но каталога `default_generated` на `main` нет),
`SteamDatabase/GameTracking-CS2` (2 227 PNG, ни одного рендера финиша),
npm-пакеты `@ianlucas/cs2-lib` (только данные) и `@skinhub/cdn` (CDN
недоступен), релизы `ianlucas/cs2-lib-assets` (архив разрезан на
`.tar.part*`, десятки гигабайт всех ассетов игры).

**Выбраны два источника, оба по HTTPS с `raw.githubusercontent.com`:**

| Что | Откуда |
|---|---|
| Изображения | [`menschma1er/cs2-images`](https://github.com/menschma1er/cs2-images) — дерево `panorama/` из `pak01_dir`, распакованное [ValveResourceFormat](https://github.com/ValveResourceFormat/ValveResourceFormat). В каталоге `default_generated` 5 481 рендер. |
| Данные | [`ByMykel/CSGO-API`](https://github.com/ByMykel/CSGO-API) — `skins.json`, 2 126 записей: рыночное имя плюс внутренние идентификаторы Valve. |

### IMAGE

Это те самые файлы, которые рисует сам клиент CS2 в инвентаре и в меню
закупки, — рендеры конкретного финиша, 512 × 384, с прозрачным фоном.
Не иконка, не миниатюра, не превью коллекции.

Имя файла собирается из идентификаторов Valve, а не подбирается по
похожести:

```
{weapon.id}_{pattern.id}_{износ}.png

weapon_ak47 + cu_ak47_cobra + light
  → weapon_ak47_cu_ak47_cobra_light.png   ← AK-47 | Redline
```

`pattern.id` — это имя paint kit'а, поэтому скин может получить только
своё собственное изображение. Если файла нет, строка помечается
`missing`, а не подставляется «похожая» картинка. Из 2 126 скинов
датасета правило разрешает 1 742; все 63 скина каталога Zevora — в их
числе.

`light` / `medium` / `heavy` — это степени износа (Factory New →
Battle-Scarred). Каталог использует `light`, самый чистый; остальные
доступны через `--wear`.

Ничего не сгенерировано, не нарисовано и не заменено плейсхолдером.
Процедурный SVG (`components/art/SkinArt.tsx`) остался **только**
обработчиком `onError` — при нормальной загрузке он не появляется ни на
одной странице (счётчик в `verify:browser` это подтверждает).

### CACHE

Изображения **скачиваются и лежат локально** в `public/skins/` —
63 файла WebP, 1,2 МБ на весь каталог. Так и сделано по умолчанию:
источник не запрещает загрузку (в репозитории нет ни лицензии, ни
ограничений), а `raw.githubusercontent.com` не предназначен для раздачи
трафика продакшен-сайта, поэтому хотлинк был бы неправильным выбором.

```bash
npm run import:skins                    # скачать, проверить, сконвертировать
npm run import:skins:cdn                # не скачивать, оставить ссылки
npm run import:skins -- --force         # переимпортировать даже valid
npm run import:skins -- --wear heavy    # другой износ
npm run import:skins -- --png           # без конвертации в WebP
npm run skins:snapshot                  # пересобрать снимок для сида
```

PNG → WebP конвертирует `sharp` (73 КБ → 11 КБ на файл без потери
альфа-канала). Если `sharp` не установлен, импортёр сохраняет PNG.

В БД хранится и то, и другое: `skins.image_url` — путь, который
загружает сайт (`/skins/ak-47-redline.webp`), а `skin_images` — журнал,
где рядом лежит исходный URL рендера с HTTP-статусом, content-type,
размером и разрешением. Снимок `lib/server/skin-images.json` позволяет
свежему клону показать картинки сразу, без запуска импорта.

Импортёр идемпотентен: строки `valid` пропускаются (если нет `--force`),
`broken` перепроверяются всегда, отсутствующие в датасете помечаются
`missing`.

Counter-Strike 2 и все изображения предметов — собственность Valve
Corporation. Zevora с Valve не связана; для коммерческого запуска права
на использование ассетов нужно получать отдельно.

### TEST

```bash
npm run verify:images              # 20 эталонных скинов
npm run verify:images -- --all     # весь каталог
npm run verify:browser             # те же 20 в настоящем браузере
```

`verify:images` делает три независимые проверки. **Происхождение**: имя
файла пересобирается из `weapon.id` и `pattern.id` датасета и должно
совпасть с тем, что записано в журнале, — подмена «иконка AK-47 вместо
AK-47 | Redline» здесь падает, потому что paint kit не сойдётся.
**Файл**: изображение читается (с диска или по сети) и должно оказаться
настоящим — статус, content-type, размер в байтах и разрешение из
заголовка самого файла. **Уникальность**: у двух скинов не может быть
одного изображения.

```
                         20 эталонных      весь каталог
происхождение            20 верных, 0      63 верных, 0
файл                     20 загрузилось    63 загрузилось
уникальность             своё изображение у каждого скина
```

Каждый из 20 эталонных скинов — `200 image/webp 512×384`, от 11 до
22 КБ. Проверено и на чистой установке, где база собрана только из
снимка, без запуска импортёра.

`verify:browser` открывает настоящий Chromium, проходит главную, все
страницы кейсов, инвентарь и историю, прокручивает их до конца и считает
по каждой странице картинки скинов, декодированные браузером и
SVG-фолбэки:

```
страница                          скинов  декодировано  SVG-фолбэк
главная + последние выигрыши          19            10            0
каталог /cases                        72            72            0
страницы кейсов (24 шт.)          77…87         все            0
инвентарь                             40            40            0
история открытий                      40            40            0

эталонных скинов отрисовано:      20/20
всего разных скинов на страницах: 63
уникальных URL:                   63
один URL на несколько скинов:     0
SVG-фолбэков:                     0
```

Отдельно проверено открытие кейса в браузере: в ленте прокрутки 79
картинок, 15 разных скинов, все декодированы; на экране результата
рендер выигранного предмета 512×384 в блоке 384 px. Счётчик фолбэков не
бутафорский — при принудительно заблокированных изображениях та же
страница показывает 36 фолбэков, при рабочих — 0.

### Связь с кейсами

```
case → case_items.skin_id → skins.image_url → рендер Valve
```

`image_url` читается одним и тем же полем в каталоге кейсов (карточка
показывает три самых дорогих предмета кейса), на странице кейса, в ленте
открытия, в инвентаре, в истории дропов, в карточке предмета и в блоке
«Последние выигрыши» — отдельных источников картинок нет.

---

## Админка

`/admin` → вкладка **Кейсы**. Создание кейса занимает пару минут и не
требует правок кода:

```
Создать кейс → название, цена, описание
   → вкладка «Предметы» → Добавить скин → поиск → вес → Добавить
   → кейс сразу доступен на /cases
```

Редактор показывает сумму весов и пересчитывает шанс каждого предмета на
лету, а рядом — среднюю ценность и маржу, с предупреждением, если кейс
уходит в убыток.

Вкладка **Статистика** сравнивает «ожидание» (доля из таблицы весов) с
«фактом» (доля реальных выпадений) за сегодня / неделю / месяц / всё
время. Расхождение на малых выборках нормально и сокращается с ростом
числа открытий.

Вкладка **Редкости** позволяет завести свою редкость: цвет, эффект,
вес по умолчанию, порядок. Шкала — это строки в таблице, а не enum.

Кейс, который уже открывали, нельзя удалить безвозвратно (на него
ссылается история) — только архивировать.

---

## Партнёрская программа

Статус выдаётся **только владельцем** через `/admin → Partners`. В API
нет ни одного пути, позволяющего выдать партнёрку себе: и `PUT`, и
`PATCH`, и `DELETE` закрыты `requireOwner`. Уровни (`Partner`, `Creator`,
`Elite Partner`, `Zevora Ambassador`) назначаются вручную, автоматического
повышения нет.

Партнёр получает бейдж, персональный промокод (реально работающий на
странице «Бонусы»), партнёрскую ссылку и доступ к закрытым кейсам,
скрытым из общего каталога.

Реферальная атрибуция пока не реализована, поэтому в Partner Lounge
показывается **настоящая активность аккаунта** (открытия, оборот,
выигрыш), а не выдуманные переходы и регистрации.

---

## Тесты

```bash
npm run test:api          # 107 интеграционных проверок на чистом сервере
npm run test:probability  # χ² тест согласия, 1 000 000 розыгрышей на кейс
npm run test:simulate     # сквозной прогон по HTTP + сверка балансов
npm run verify:browser    # изображения скинов в настоящем браузере
```

**Что проверено**

*Открытие.* Списание ровно на цену кейса; выпавший предмет всегда из
таблицы кейса; предмет появляется в инвентаре; возвращается билет
розыгрыша.

*Двойной клик и повторы.* Повтор с тем же ключом возвращает то же
открытие и не списывает второй раз. 8 одновременных запросов с одним
ключом дают одно открытие и одно списание. Одинаковый ключ у разных
пользователей не конфликтует.

*Гонки.* При балансе на N открытий запускается N+25 параллельных
запросов: успешных не больше N, лишние получают 402, баланс не уходит в
минус, число предметов равно числу открытий.

*Подмена.* Поля `price_minor`, `skin_id`, `win`, `force` в теле
игнорируются — списывается цена из БД. В магазине списывается серверная
цена с наценкой, а не присланная.

*Доступ.* Открытие без сессии → 401. Админские эндпоинты обычному
пользователю → 403. Самоназначение партнёрки → 403. Продажа чужого
предмета → 409, предмет остаётся у владельца. Глобальная история закрыта
от игрока.

*Инъекции.* Пять вредоносных payload'ов в slug, мусорный `sort` и
`rarity`, кавычки в названии кейса, строка вместо массива id, дробный и
переполненный id, битый JSON — ничего не ломает выборку и не роняет
сервер.

*Вероятности.* χ² тест согласия при 1 000 000 розыгрышей на каждый из 7
кейсов (7 млн розыгрышей): все проходят при p = 0.001. Самый редкий
предмет — ★ Sport Gloves | Pandora's Box, p = 0.0284%.

*Сверка.* 1 200 открытий по HTTP: баланс сходится с суммой списаний до
копейки, число предметов равно числу открытий (1 200), стоимость
инвентаря равна сумме выпавшего (172 049,42 ₽ в обе стороны), история
содержит все записи, каждый выпавший предмет есть в таблице своего
кейса.

*Изображения.* Происхождение, файл и уникальность — 63 из 63 по всему
каталогу. В браузере отрисованы все 20 эталонных скинов, 63 разных скина
с 63 разными URL, ни одной картинки, общей для двух скинов, ни одного
SVG-фолбэка.

*Интерфейс.* Полный прогон в браузере: вход, каталог, открытие с
анимацией, инвентарь, история, админка. Создание кейса через UI и его
открытие — 18 секунд. Отсутствие горизонтального скролла на 12 страницах
при ширине 390 px.

---

## GitHub Pages

Само приложение на Pages не поедет и не должно: розыгрыш намеренно живёт
на сервере — SQLite, сессии, транзакционное списание, CSPRNG за API, — а
Pages отдаёт только статику.

Поэтому туда публикуется витрина: `npm run build:showcase` собирает
`docs/` — 25 кейсов с настоящим артом, их реальные дроп-таблицы из
`cases.json` и анимация открывания. На самой странице прямо написано,
что розыгрыш там клиентский и ни на что не влияет.

Деплой делает `.github/workflows/pages.yml` при пуше: пересобирает арт,
таблицы и витрину из исходников, чтобы опубликованное не разошлось с
репозиторием. Если Pages в настройках репозитория ещё не включены,
воркфлоу включает их сам (`actions/configure-pages` с `enablement: true`);
источник должен стоять «GitHub Actions».

Рабочее приложение по-прежнему требует Node-хостинга.

---

## Оговорки

Демонстрационная сборка: баланс и предметы виртуальные, платежи не
проводятся, вывод в Steam имитируется. 18+.

Counter-Strike 2 и Steam — товарные знаки Valve Corporation. Zevora не
связана с Valve Corporation. Названия кейсов, дроп-таблицы и айдентика —
оригинальные; имена скинов соответствуют реальным предметам CS2.
